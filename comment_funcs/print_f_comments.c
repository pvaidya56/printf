void printf(const char * const str, ...)
{
	//set variables
	//cycle through char by char until we hit %
	//scan for valid spacers until we hit flag
	//add element count from % to flag to @offset
	//identify flag and return function corresponding to flag
	//loop
}
