void (*id_flag(char *s))(char id)
{
	//create array with allowed spacers
	//create struct with allowed id flags
	//scan string after % for valid spacers
	//on invalid spacer, check struct for id flag
	//if there is a match, return the correct function to add to buffer based on id
	//or function to add invalid id flags to buffer with %
}
